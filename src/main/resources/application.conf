akka {
  loglevel = INFO
}

http {
  host = "0.0.0.0"
  port = 9000
}

db.postgres {
  driver = "slick.driver.PostgresDriver$"
  db {
    url = "jdbc:postgresql:hodor"
    driver = org.postgresql.Driver
    user = "admin"
    password = "admin"
    host = "0.0.0.0"
    port = 27017
  }
}
